# State — <feature name>

**Slug:** <feature-slug>
**Created:** YYYY-MM-DD
**Last updated:** YYYY-MM-DD

## Current phase

**Phase:** requirements
**Status:** in-progress

Phase is one of: `requirements`, `lld`, `hld`, `plan`, `implement`, `done`.
Status is one of: `not-started`, `in-progress`, `awaiting-confirmation`, `done`.

## Phase history

- [ ] requirements
- [ ] lld
- [ ] hld        <!-- delete this line if HLD was skipped; note why in the entry below -->
- [ ] plan
- [ ] implement

## Resume pointer

<!-- The single fact /klaus needs to resume without re-deriving it.
     Requirements phase: the question ID currently pending, e.g. "requirements-qa.md, Q4".
     LLD phase: which sub-stage — "entities" or "class-design".
     Implement phase: the task ID currently in progress, e.g. "plan.md, T3". -->

Nothing yet — phase just started.

## Notes for /klaus

<!-- Anything that would otherwise force /klaus to re-derive context on resume:
     decisions that closed off an alternative, a scope change mid-phase, a skip
     with its reason ("HLD skipped — single-process CLI tool, no network"). -->
